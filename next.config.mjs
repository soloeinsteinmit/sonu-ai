/** @type {import('next').NextConfig} */
const nextConfig = {
  // No custom webpack configuration needed for this approach.
  // The model is now a static asset in the 'public' directory.
};

export default nextConfig;
